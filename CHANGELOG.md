# 随手 CHANGELOG

## v1.7.0（2026-08-23）

### 命令系统

- **命令面板**：`Ctrl+Shift+P` / `⇧⌘P` 唤出 —— 模糊过滤、↑↓、Enter 执行、点击空白/Esc 关闭
- 覆盖 文件/导出/视图/主题/排序/插入/帮助 全类别约 26 条命令

### 菜单中文化

- Info.plist 声明 `CFBundleDevelopmentRegion=zh-Hans` + 启动早期 `AppleLanguages` 锁定
- 系统菜单组（文件/编辑/显示/窗口/帮助）与自定义菜单全中文

### 附件系统完善

- **通用文件附件**：任意文件（PDF/DOCX/ZIP…）粘贴/拖入/`⌥⌘P` 面板入库 `attachments/<noteId>/`，插入 `[文件名](…)` 链接
- **附件面板**：图片缩略网格 + 文件列表（打开/复制引用/Finder/删除），兼容旧 `images/`
- 预览点击附件链接 → 系统默认程序打开（路径 URL 编解码闭环）

### 性能

- 图片内联前统一重采样（>1600px 缩放；无透明转 JPEG q0.8）—— **Web Content 370MB → 80MB**
- 本地/远程图片 dataURL 缓存 LRU 封顶（24/16 张）
- 预览渲染智能降频（文档 20K/60K/100K → 0.30/0.45/0.60s）
- 行号标尺行数仅文本长度变化时重算

### 细节

- 仓库清单清理（37 个导入误建分类 → 移除空文件夹/全清校验）
- 同目录重名校验（笔记/文件夹全入口）+ 轻提示 Toast（替代模态弹窗）
- Tab = 2 空格；callout 紧凑语法与 emoji 简码优先级修复
- 删除后自动选中下一项（VSCode 游标）；路径编码还原打开附件

## v1.6.0（2026-08-23）

### 多选与撤销

- **多选机制（VSCode 资源管理器范式）**：List 升为多选 —— 单击=选中并打开、**双击打开**、`⇧+点击`区间选、`⌘+点击`增减、**`⇧+⬇` 逐行扩展 / `⇧+⬆` 撤退选中**（系统原生键盘多选）
- **批量删除**：`⌘⌫` / 工具栏删除作用于全部选中 → 统一进回收站
- **删除撤销**：删除后侧栏浮出"已删除 · 撤销"条（6 秒，点击一键恢复全部）；回收站面板加「恢复全部」
- 编辑器 `⌘Z` / `⇧⌘Z` 文本撤销保持原生

### 导入能力

- 批量/单个文件导入（面板多选）＋「导入到指定文件夹…」（目标 Sheet：根/各文件夹）
- **导入文件夹（递归）**：直接文件归"文件夹名"分类（同名复用），子文件夹各自成分类
- **拖拽到文件夹行**：直接导入该分类（悬停高亮）

### 其他

- **复制图片直接粘贴**：4 级剪贴板提取（Finder 文件/PNG·TIFF/现代 provider 型/NSImage 兜底）→ 存 `images/<id>/` → 插入引用
- **App 图标**：深底青→紫渐变 `</>` 极简代码块设计；软件内图标多彩化（活动条渐变圆底、文件夹分类色）
- **导出**：菜单一键直达 `~/Downloads/随手导出/`（同名自动序号）＋导出成功/失败弹窗；修复 KVC 崩溃与渲染器生命周期；帮助菜单「导出诊断…」
- 工具栏"仅编辑"图标去重（`text.cursor`）

## v1.5.2（2026-08-22）

### 资源管理器 0 构型标记：完全 VSCode 化

- **移除「未分类」组** —— 未归档笔记直接显示在根下（VSCode 工作区根文件的语义）
- 时间排序模式：**文件夹在前**（按名称排序）+ 根文件在后（按所选时间序）
- 手动排序模式：整体平铺，顺序完全由 `.order.json` 控制
- 文件夹菜单文案同步（"删除文件夹 → 笔记移到根目录"）

## v1.5.1（2026-08-22）

### 排序机制响应式重构

- **移除"打开笔记强制置顶"** —— 绝不干扰用户排序（保存/打开均不再重排）
- 三种排序：**按更新时间 / 按创建时间 / 手动排序**（footer 右侧箭头菜单）
- **手动排序持久化**：`notes/.order.json`（与 start 兼容的点文件）；右键笔记「上移 / 下移」自动切到手动模式并落盘；新建/删除时序列自动维护（未列入的笔记按更新时间附尾）
- 全量构建修复：`NoteIndexItem` 补 `created` 字段（排序依赖 + 消除此前被增量缓存掩盖的真实编译错误）

## v1.5.0（2026-08-22）

### 致命修复

- **列表点击切换笔记失效**：`openNote` 的 guard 基于 `selectedNoteID`（List 选择会先写入），导致二次点击被恒拦截、内容永远留在第一篇。引入 `loadedNoteID`（已装载态）与选中态分离，切换恢复；删除/外部失效路径同步清理。已加"切换压力测试"到 `--verify`。

### 界面（VSCode 资源管理器风格）

- 侧栏改为 **资源管理器树**：面板标题行（`▾ 笔记` + `+` 菜单：新建笔记/新建文件夹）→ 紧凑搜索行 → **文件夹树**
- **文件夹 = 分类**：chevron 折叠、色点、计数、右侧 folder 菜单（重命名/新建笔记到此/删除）；未分类为根文件夹
- 移除搜索/分类 chips/分组开关三层结构（消灭留白与歧义）
- 搜索时树转为平铺命中结果（标题优先 + 关键词高亮）

## v1.4.0（2026-08-22）

### 设计重构（VSCode 式侧边栏）

- 侧边栏改为 **活动条 + 面板** 结构：左 36px 图标条（笔记 / 回收站），右侧面板切换 —— 强逻辑性、入口收拢
- 回收站成为内嵌面板（不在弹窗中）：列表 / 恢复 / 彻底删除 / 清空

### 导出修复

- 根因：离屏 WKWebView 未挂载窗口 → `loadFileURL` 永不完成。修复：导出时临时挂到主窗口树（屏幕外）
- 增加 15s 超时保护、加载失败显示错误、成功提示 + 「在 Finder 中显示」

### 导入与图片

- 导入治理：BOM 剥离、CRLF/CR → LF 规整、YAML front matter 解析 `title:`、正文首行 `# 标题` 提升为笔记标题并移除
- 「插入图片」改为文件选择对话框（可多选）→ 自动存入 `images/<noteId>/` → 插入引用（模拟粘贴路径一致）

### 健壮性

- 窗口激活时刷新索引（跨应用改动兜底）+ 打开失败自愈提示 + FS watch 60s 心跳重链

## v1.3.0（2026-08-22）

### 编辑器

- **Markdown 语法着色**：标题/粗斜体/行内码/链接/引用/列表(任务/数字)/代码围栏/表格分隔/公式/高亮删除线标记 —— 后台解析、350ms 防抖，>500KB 自动跳过；不污染撤销栈
- **「插入」菜单**：链接(⌥⌘L)/行内代码(⌥⌘C)/代码块(⌥⌘K)/公式(⌥⌘F)/表格(⌥⌘T)/图片引用 —— 在光标处插入语法，选中文本作为内容主体

### 组织与安全

- **回收站**：删除笔记移入 `notes/.trash/`（含历史版本目录），侧栏垃圾桶入口 → 恢复 / 彻底删除 / 清空；与 start 数据目录兼容（点文件忽略）
- **按分类分组视图**：侧栏文件夹开关分组显示（未分类 + 各分类组），组头可折叠，保持置顶/搜索行为

### 预览

- **联网图片**：正文 `http(s)` 图片 → 下载（并发 4 / 单张 ≤8MB / 每文档 ≤32 张）→ dataURL 内联缓存（含磁盘缓存 `assets/.cache/`），下载完成自动触发重渲染
- **预览字体设置**：系统 / 苹方 / 楷体 / 宋体 / 等宽

### 修复

- 分类从未加载（init 缺 `loadCategories`，刷新索引时同步）—— 早期发现并根治

## v1.2.0（2026-08-22）

### 交互与体验（大版本）

- **当前编辑的笔记固定列表首位** —— 自动保存刷新排序不再导致列表乱跳
- **全文搜索**：正文级检索（增量全文缓存，标题命中优先，空格分词，`N/M 匹配` 提示 + Esc 清空）
- **光标位置记忆**：切回任意笔记自动回到上次编辑处（滚动至可视区）
- **预览滚动跟随**：编辑器光标移动 → 预览按最近标题定位（处理同名标题出现序号）
- **窗口标题跟随当前笔记**；状态栏增加光标 **行:列**
- **编辑器手感**：当前行高亮底纹、回车自动延续列表/数字列表/引用前缀、Tab 插入 4 空格
- **侧边栏层次**：分类色点（未分类灰点）、行尾分类名小字、搜索词 accent 高亮
- **菜单组织**：「视图」菜单（三种模式 + 快速换主题）、「导出」菜单、Help「快捷键一览…」、Apple 菜单「关于 随手…」
- **快捷键**：⌥⌘↑/⌥⌘↓ 上一篇/下一篇笔记
- **新建聚焦**：新建/切换笔记自动聚焦编辑器
- **导入去重**：同名文件自动加序号（名字 2）

### 健壮性与性能

- 大文档（>200KB）预览渲染降频（500ms 防抖）
- **修复：分类从未加载**（init 缺 loadCategories；刷新索引时同步，外部修改分类即时生效）
- 数据层错误（读取失败等）通过统一 alert 展示

### 工程

- 图标：主体换为微软 Fluent UI Emoji 3D Frog（MIT），两级 Lanczos 插值放大
- `build-app.sh` 自动部署到 `~/Applications/随手.app`（避开桌面路径的开发期 TCC 询问）
- 调试参数：`--open-note <id>` `--search-query <词>` `--verify` `--probe-preview`

## v1.1.0（2026-08-22）

- 重命名「随手」；新图标（Fluent 3D 蛙）
- 性能：编辑器指纹比较、图片 data URL 缓存（mtime）、hljs/mermaid 渲染缓存、ImageIO 缩略图、WKWebView handler 反注册

## v1.0.0（2026-08-22）

- 首个可用版本：SwiftUI + AppKit 原生 Markdown 编辑器
- 分屏实时预览、自动保存（500ms）、版本快照（10 份）、分类、导出（PDF/MD/TXT）
- 与 start 项目 JSON 数据格式完全兼容
